# Release Notes

## v1.0.1 - Accessibility hardening

- Increased contrast for secondary, success, warning, and failure text across the event and replay workbench.
- Made the horizontally scrollable contract registry keyboard focusable and labeled.
- Preserved retry, idempotency, contract, import/export, and human replay workflows.

## v1.0.0 - Webhook Reliability Workbench

- Added ten synthetic events across five delivery states.
- Added six versioned webhook contracts with path and primitive-type preflight.
- Added deterministic healthy, HTTP 429, HTTP 503, timeout, and HTTP 401 injections.
- Added fixed, linear, and exponential retry timing.
- Added default-on idempotency protection with HTTP 208 no-op evidence.
- Added reasoned human replay approval that preserves source events.
- Added payload, contract, attempt, HTTP preview, contract registry, and audit views.
- Added a Canvas event-state timeline.
- Added local JSON import and portable JSON evidence export.
- Added logic, repository, desktop/mobile browser, accessibility, privacy, screenshot, load-failure, and deployed-test paths.

All content is synthetic. The release sends no webhook and claims no production result.
